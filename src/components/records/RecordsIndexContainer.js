import { connect } from 'react-redux';
import RecordsIndex from './RecordsIndex';

const mapStateToProps = () => ({
});

const mapDispatchToProps = () => ({
});


export default connect(mapStateToProps, mapDispatchToProps)(RecordsIndex);

